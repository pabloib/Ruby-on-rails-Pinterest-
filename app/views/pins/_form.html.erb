<%= form_for(@pin) do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="form-group">
  <div class="field">
    <%= f.label :name, "Nombre" %><br>
    <%= f.text_field :name, :class => 'form-control', :placeholder => "Nombre del Pin" %>
  </div>
</div>

<div class="form-group">
  <div class="field">
    <%= f.label :photo, "Foto" %><br>
    <%= f.text_field :photo, :class => 'form-control', :placeholder => "Url de la foto" %>
  </div>
</div>

<div class="form-group">
  <div class="field">
    <%= f.label :description, "Descripción" %><br>
    <%= f.text_area :description, :class => 'form-control', :rows => "5", :cols => "30" %>
  </div>
</div>

  <div class="actions">
    <%= f.submit "Crear", :class => "btn btn-primary" %>
  </div>
<% end %>
